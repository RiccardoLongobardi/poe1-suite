# Fixtures live in tests/conftest.py. This file is intentionally empty
# so the package dir stays a valid Python namespace parent (needed so
# tests/conftest.py gets a unique ``packages.builds.tests.conftest``
# module name under pytest's importlib mode, avoiding a collision with
# ``packages.pricing.tests.conftest``).
