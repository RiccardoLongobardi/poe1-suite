# CLAUDE_PERPLEXITY_WORKFLOW
Coordination playbook between **Perplexity** (research / design / data-source surveys) and **Claude Code** (in-repo implementation) for the `poe1-suite` mono-repo.

This file's only job is to keep the two tools in sync — what each is responsible for, what's currently open, what's been decided. The source of truth for the codebase itself remains [`CLAUDE.md`](./CLAUDE.md) (architecture, conventions, gate, lessons learned).

> **Read this AND `CLAUDE.md` before starting any session.** This file is the workflow contract; `CLAUDE.md` is the project contract.

---

## 1. Where the project actually stands (read first)

Don't trust earlier versions of this file — the section below is the authoritative snapshot. As of **2026-05-22**:

- **FOB is live in production**, free tier:
  - Frontend: <https://fob-ten.vercel.app> (Vercel, auto-deploy from `main`).
  - Backend: <https://fob-api-rtgg.onrender.com> (Render, region Frankfurt, auto-deploy from `main`).
  - Cost: **$0/month**.
- **Baseline gate**: 777 tests green / 145 mypy / ruff clean.
- **Working features (all QA-verified or post-QA fixed)**:
  - Build Finder with class/asc/stat-floor/sort filters + natural-language extraction (Step 15) + per-ascendancy population stats panel (Step 19). ✅
  - Planner with 6-stage `BuildPlan`, SSE streaming progress + ETA. ✅
  - "Importa stage in PoB": exports a stage-specific PoB code. ✅
  - PoB Analyze → full build dashboard: character header + key stats, equipment grid with per-item tooltips, flasks, tree jewels, skill-link panel. ✅
  - Cold-start Divine Orb warmup overlay. ✅
  - Trade dialog: `TradeSearchDialog` with full GGG stat DB (~9.5k stats), name/base search, per-mod toggles + strictness slider, 5L/6L filter, Instant Buyout default, integer min-roll filters, domain-aware implicit/explicit stat resolution. ✅
  - Theorycrafter Build Generator v2 (Step 40 + Step 41 fixes) — form-driven UI, graph engine, anti-hallucination asserts, complete PoB export (5 gem groups, real affix lines, flasks, jewels, correct class start). ✅
  - Theorycrafter gear cards expandable + Trade dialog per slot (Step 42). ✅
  - Theorycrafter viability report (Step 43) + connected BFS passive tree path (Step 44) + full-budget tree fill ~120 nodes (Step 45a) + deduplicated gem layout with compatible supports (Step 45b) + 3.28 Awakened gem allowlist (Step 45c) + realistic per-slot item affixes (Step 45d) + localised tree pathing (Step 46) + real item mods via RePoE (Step 47) + tree masteries / clean nodes / honest estimates (Step 48) + value-per-point tree optimisation (Step 49). ✅
- **Design system**: "Void Stone & Ember" — void-black warm backgrounds, ember-gold accent, parchment text, Cinzel/Cabinet Grotesk/Geist Mono type. Light mode: "Parchment" (warm cream + ink). Both QA-verified. ✅

**Baseline gate (current): 777 tests green / 145 mypy / ruff clean.**

If anything you read in this file or in `CLAUDE.md` contradicts the above, **the above wins**.

---

## 1bis. Where to verify the *current* state (read before any planning)

The §1 snapshot is hand-maintained — it might lag a few hours after a feature lands. Before drafting prompts, doing research, or assuming anything about the codebase, **always re-check the live sources** below.

### Repo (GitHub, branch `main`)

- **Browse the repo**: <https://github.com/RiccardoLongobardi/poe1-suite>
- **Latest commit on main**:
  ```sh
  curl -s https://api.github.com/repos/RiccardoLongobardi/poe1-suite/commits/main | jq -r '"\(.sha[0:7]) \(.commit.author.date) \(.commit.message | split("\n")[0])"'
  ```
- **Recent commit log** (last 20):
  ```sh
  curl -s 'https://api.github.com/repos/RiccardoLongobardi/poe1-suite/commits?per_page=20&sha=main' | jq -r '.[] | "\(.sha[0:7])  \(.commit.message | split("\n")[0])"'
  ```
- **Raw file at HEAD**:
  ```
  https://raw.githubusercontent.com/RiccardoLongobardi/poe1-suite/main/CLAUDE.md
  https://raw.githubusercontent.com/RiccardoLongobardi/poe1-suite/main/CLAUDE_PERPLEXITY_WORKFLOW.md
  ```
- **File tree at HEAD**:
  ```sh
  curl -s 'https://api.github.com/repos/RiccardoLongobardi/poe1-suite/git/trees/main?recursive=1' | jq -r '.tree[].path' | head -80
  ```

### Live backend (Render)

- **Health probe**: `curl -s https://fob-api-rtgg.onrender.com/health | jq .`
- **First request after ~15 min idle takes ~30 s** (Render free-tier cold start — the Divine Orb overlay handles this).

### Live frontend (Vercel)

- <https://fob-ten.vercel.app> — `/finder`, `/analyze`, `/planner`, `/theorycrafter`.

---

## 2. Stack & data sources (no PostgreSQL, no ETL)

| Layer | Source | Caching | Refresh |
|---|---|---|---|
| Live economy | `poe.ninja` economy JSON | `diskcache` 15 min TTL | Per-request |
| Build ladder | `poe.ninja` builds protobuf | `diskcache` 15 min TTL | Per-request |
| Trade search | GGG `/api/trade/search` via `POST /fob/trade-url` | in-memory 8 min TTL | Backend POST works from Render; frontend opens returned URL |
| Passive tree | GGG vendored JSON | `packages/fob/data/tree/3_28.json` | Manual per league |
| Item bases | repoe-fork JSON | `packages/fob/data/items/base_items.json` | Manual per league |
| Gem data | PoB Community fork (`src/Data/Skills/*.lua`) | `packages/fob/data/gems/gems_3_28.json` (generated by `scripts/extract_gems.py`) | Manual per league |
| Item mods | repoe-fork (`mods.json` + `stat_translations.json`) | `packages/fob/data/mods/mods_3_28.json` (generated by `scripts/extract_mods.py`) | Manual per league |
| Unique items | PoB Community (`Data/Uniques/*.lua`) | `packages/fob/data/uniques/uniques_3_28.json` (generated by `scripts/extract_uniques.py`) | Manual per league |

Sources explicitly rejected (don't propose again): poedb.tw, GGG OAuth API for game data, brather1ng/RePoE (dead), hand-authored gem lists (no fallbacks — use official PoB data only).

---

## 3. Roles

### 3.1 Perplexity — research & design

Owns: data-source surveys, algorithm design, comparative library reviews, long-form research, UI/UX design direction and design system spec.

Does NOT: edit `.py` / `.ts` / `.json` files, modify Claude Code todos, update §6 implementation status, analyse bugs or write fix strategies (that is Claude's job).

### 3.2 Claude Code — implementation

Owns: all code under `packages/` + `apps/` + `scripts/`, test changes, gate enforcement, commits + pushes, updating both `CLAUDE.md` and this file's §6 / §7. Also owns bug root-cause analysis and fix strategy.

Constraints: no `--no-verify`, no secrets, must run full gate before declaring done.

### 3.3 The user (Riccardo)

Owns: strategic direction, manual QA in PoB Community, final-call on architectural trade-offs.

---

## 4. Collaboration rules

1. `CLAUDE.md` is the contract, this file is the playbook. When they conflict, `CLAUDE.md` wins.
2. No silent re-architecture — changes to stack / data sources / public API go in §7.
3. Prompts in this file are reusable templates, self-contained, runnable today without past-chat context.
4. **GGG Trade access (corrected 2026-05-18):** the Render backend **can** reach GGG — `POST /fob/trade-url` mints prefilled search URLs and `scripts/extract_trade_stats.py` vendors `/api/trade/data/stats`. What does NOT work: a browser-side `fetch` to GGG's Trade API (CORS) and navigating the browser straight to a `pathofexile.com/api/` path (Cloudflare 403, `code 6`). GGG Trade calls go through the backend; the frontend only opens the URL the backend returns.
5. Vendor data, don't fetch at runtime.
6. **The Patch Notes page is part of every feature.** Whenever a feature/fix ships, the `RELEASES` array in `apps/shell/src/pages/PatchNotesPage.tsx` MUST be updated in the **same commit** as `CLAUDE.md` and this file — with user-facing, bilingual (`it`/`en`) copy. A step that updates the `.md` files but not the Patch Notes is not done.
7. **No fallbacks to hand-authored data.** If a parse script fails or official data is incomplete, Claude must stop and report — never substitute invented or manually-curated data as a workaround.

---

## 5. Open questions for Perplexity

*(none as of 2026-05-22)*

---

## 6. Backlog & status

### IN PROGRESS

- **Theorycrafter "deep" optimiser — PoB-exact fitness** (initiative, Steps 50+). Goal: generate viable + OP builds from scratch by optimising against PoB's *real* calc, not a keyword proxy. Architecture chosen by Riccardo: hybrid objective (calc + ladder prior), as exact as PoB → run PoB headless; heavy compute lives in (a) offline precompute + (c) a local tool (NOT live on Render).
  - **Step 50 (DONE 2026-05-24): spike + evaluator foundation.** Spike verdict: lupa can't host PoB's native modules (ABI clash → crash); Docker/WSL absent; **ctypes on PoB's bundled `lua51.dll` works, zero installs**. Shipped `scripts/setup_pob.py` (fetch runtime → `.pob_runtime/`, gitignored) + `scripts/pob_eval.py` (`PobEvaluator.evaluate(pob_code) -> exact stats`, ~280 ms/eval). Validated: Marauder/Jugg Cyclone → Life 5548 / FullDPS 7274 (exact).
  - **Step 51 (DONE 2026-05-24): the optimiser.** `scripts/optimize_build.py` — fitness = real DPS × viability penalty; 6L support forward-selection + connectivity-preserving tree swap local search over `PobEvaluator`. Result on Cyclone: support opt +12 % FullDPS / +65 % TotalDPS; **tree already near-optimal** (greedy hits a PoB local optimum); **the DPS ceiling is GEAR** — our gear recs omit flat added-damage mods (the #1 weapon DPS source). No Patch Notes (internal). 756 tests / 137 mypy / ruff clean.
  - **Step 52 (DONE 2026-05-24): GEAR flat added-damage mods.** `mods_3_28.json` gained an `added` section (15 groups / 341 tiers, 2-stat `Adds # to #` mods); `realmods._added_line` resolves them; `_stat_priorities` puts the flat-added stem first on the weapon (+ attack variants on jewellery/gloves, spell-to-spells on caster weapons). Measured (PoB-exact, Marauder/Jugg Cyclone): **FullDPS 7274 → 16283 (+124 %)**. User-facing (Patch Notes). 757 tests / 137 mypy.
  - **QA sweep (DONE 2026-05-24): `scripts/qa_sweep.py`** — generates + PoB-evaluates 27 archetypes, classifies each. Result: **13 OK, 5 LOW_POOL (ES builds ~3k ES — too low), 4 WRONG_WEAPON (elemental melee/bow attacks get a 2H sword / wand instead of the right weapon + wrong damage stats — confirmed the `is_spell = damage∈{fire,cold,lightning}` mis-classification), 3 LOW_RES (chaos ES builds undercap one res ~61), 2 LOW_DPS (minion builds calc ~0 DPS — minion damage isn't set up).** Physical attacks + most spells are solid (Vortex 92k, Fireball 64k FullDPS). Data-driven priority for the next fixes ⤵
  - **Step 53 (DONE 2026-05-25): elemental-attack classification.** `_stat_priorities` now classifies attack-vs-spell by skill **tags** (`is_attack = "attack" in skill.tags`), not damage type — so Lightning Strike / Molten Strike / Frost Blades get attack stats + flat `Adds <Element> Damage` on a melee weapon, and Ice Shot resolves to a Bow (`_is_bow_skill` heuristic: `miragearchercanuse`+`rangedattack`, no `melee`/`wandattack`/`spell`, since PoB tags no bow attack with a `bow` SkillType). Spectral Throw (ranged melee-weapon attack) now gets a melee weapon, not a Wand. The qa_sweep `_verdict` heuristic was made honest (inspects the generated weapon slot / spell-stat presence instead of a static damage-type check). Sweep: WRONG_WEAPON 4 → 0, OK 13 → 16, no regressions. User-facing (Patch Notes). 758 tests / 138 mypy.
  - **Step 54 (DONE 2026-05-25): ES pool + resistance spread.** Root cause was a dead gear filter: `_DEFENCE_TAG["es"] = "energy_shield"` matched **no** base in the catalogue, so `_pick_base` fell back to the highest-drop-level (hybrid) base on which `local_energy_shield_+%` (int_armour-only) can't roll → ES dropped from helmet/gloves/boots → ~3k pool. Fixed to `"int_armour"`. Also rebalanced the resistance distribution in `_stat_priorities` (Fire was on 7 slots, Lightning on 1 → each elemental res now on ~4-5 slots). Measured: six ES casters 3.0-3.1k → 4.4-4.7k ES, all three res cap; **OK 16 → 23, LOW_RES 3 → 0, LOW_POOL 6 → 2** (the 2 remaining are out-of-scope/pre-existing: a Shadow *life* build at 744 life, and a Saboteur ES-0 calc quirk). User-facing (Patch Notes). 759 tests / 138 mypy.
  - **Step 55 (DONE 2026-05-25): minion DPS.** Two bugs: (1) minion-summon gems carry caster tags, so `_CORE_SUPPORTS` socketed Spell Echo/Conc Effect/Unleash (useless for minions) ahead of the `createsminion` supports — added `_MINION_SUPPORTS` + a minion branch in `_select_supports_raw` so Minion Damage/Feeding Frenzy/Predator/… rank first; (2) `_DAMAGE_KEYWORDS` had no minion entry, so 0 minion tree nodes were taken — added `"minion"` keywords and route minion skills to them. Summon Skeletons 515 → 4327 FullDPS. Raise Spectre stays 0 (PoB needs a *selected spectre monster*; can't pick without vendored monster data) → added a `spectre_needs_selection` viability warning. **LOW_DPS 2 → 1.** User-facing (Patch Notes). 761 tests / 138 mypy.
  - **Step 56 (DONE 2026-05-25): gear co-optimisation + precompute + live serving.** Full pipeline shipped. `optimize_build.py` gained `optimize_weapon` (tries the top weapon bases of the build's class, PoB-fitness best) — the optimiser now tunes supports + weapon + tree. `scripts/precompute_builds.py` runs it over a 5-archetype matrix and writes `data/theory/precomputed_3_28.json` (~166 KB). `poe1_fob.theory.precomputed.lookup(intent)` serves the matching optimum; `POST /fob/theory/generate` prefers it, falls back to live generation. `StatEstimate` gained `full_dps`/`total_ehp`; `BuildSkeleton` gained `optimised`; the UI shows a green "Ottimizzato con PoB" badge + real DPS/EHP. **Render never runs PoB — serves the vendored JSON only ($0 deploy intact).** Measured: Cyclone 16.8k→22.6k, Vortex 92.8k→124.7k FullDPS. User-facing (Patch Notes). 765 tests / 141 mypy.
  - **Step 57 (DONE 2026-05-25): no auto-allocated keystones.** The remaining LOW_POOL failures (Frost Blades 1 life, Lightning Trap 0 ES, Summon Skeletons low pool) all traced to the keyword scorer auto-allocating build-breaking keystones (Chaos Inoculation on a life build, Avatar of Fire / ES-reworks on an ES build). Fix: `_keystone_ids(td)` unioned into the live generator's `excluded` set → keystones dropped from targets/travel/fill. The PoB optimiser (Step 56) still adds PoB-validated keystones, so precomputed builds keep the good ones. **LOW_POOL 3 → 0, OK 23 → 26**; also raised DPS on builds that grabbed harmful keystones (Cyclone 16.8k→32.9k, Arc 25.9k→41.3k). User-facing (Patch Notes). 766 tests / 141 mypy.
- **Mirror-tier build initiative (Steps 58+).** Goal set by Riccardo (2026-05-25): the Theorycrafter must generate **complete mirror-tier builds from scratch**, like the 84M-DPS Cyclone Juggernaut at the top of the poe.ninja ladder. The PoB-exact optimiser (Steps 50-56) is the right engine — the work is to **widen its search space** (more data) and **give it the compute** to explore it. A ladder comparison (`scripts/compare_ladder.py`, Vortex Occultist) quantified the gap: our build is **19% of the ladder's DPS and 12% of its EHP**. Root causes → the 7 work items below. The ladder is used only as a *popularity/structure signal* (which uniques/jewels a top build of this archetype runs), never as the build source — the Finder/Theorycrafter boundary holds.
  - **Phase 1 — unblock + uniques (biggest lever, fail-first):**
    - **(1a) Uniques DB — DONE (Step 58, 2026-05-25).** `scripts/extract_uniques.py` → `data/uniques/uniques_3_28.json` (1254 uniques: name/base/slot/drop_level/current-variant mods); `poe1_fob.gear.uniques` loader; `scripts/compare_ladder.py` ladder-gap tool. Internal foundation, no user-facing change yet. 770 tests / 145 mypy.
    - **(1b) Uniques selection — DONE (Step 59, 2026-05-25).** `optimize_uniques` tries the top relevance-scored candidate uniques per slot (PoB-fitness gated); `unique_pob_body` renders the unique's PoB text with `(min-max)`→max so PoB applies real stats (no encoder change — reuses the multi-line `item_name` path). `_Encoder.code` got a `pob_gear` override. Re-ran precompute → huge gains: Cyclone 22.6k→110k FullDPS, Vortex 124.7k→173k (EHP 6.1k→22.9k), Lacerate→92k (+Starforge), Arc→68k, Ice Shot→32k. The optimiser autonomously found the real chase uniques (The Bringer of Rain, Mark of the Shaper, Galesight, …) — PoB-validated, not curated. User-facing. 771 tests / 145 mypy.
    - **(2) Timeless Jewel data.** Headless PoB can't currently load `Data/TimelessJewelData/*.zip` (the Cyclone 84M eval crashed) — unblock the loader so timeless-jewel builds can even be *evaluated*, then modelled.
  - **Phase 2 — layered defences — DONE (Step 60, 2026-05-25).** Root cause wasn't missing search space (defensive uniques + nodes were already reachable) — it was the *fitness*: `DPS × penalty` only gated on a pool floor, never rewarded EHP. Added a sublinear, saturating EHP reward (`0.4 + 0.6·min(EHP/target,1)^0.5`, target 12k/25k/40k). The optimiser now layers defences: Arc EHP 21.3k→40.0k (+88%) for −3% DPS (Dawnbreaker block shield), Vortex 22.9k→26.9k (Aegis Aurora), for marginal DPS cost. User-facing. 771 tests / 145 mypy.
  - **Phase 3 — non-hit damage + crafting:** **(4) DoT / ailment modelling — DONE (Step 61, 2026-05-25):** `extract_mods.py` now keeps DoT-multiplier stat ids; `_stat_priorities` leads DoT builds' weapon+amulet with "Damage over Time Multiplier" (dropping the dead flat-added-to-spells). Only the generic DoT multi rolls as a normal affix; element-specific DoT multi comes from uniques (Rime Gaze) — already in the optimiser's uniques pass. Honest scope: precomputed builds unchanged (their damage slots become uniques + FullDPS already captures DoT); the win is the **live generator's** DoT recommendations. 772 tests / 145 mypy. **Still open: (3) Cluster jewels + full mod pool, (7) multi-mod / influenced / meta-crafted rares** (mirror rares are mod *combinations*, not one mod per priority — the biggest remaining gear-quality lever for the precomputed builds).
  - **Phase 4 — jewels + buffs + scale:** **(2) Timeless Jewel — eval UNBLOCKED (Step 62, 2026-05-25):** headless PoB stubbed `Inflate`/`NewFileSearch`/`GetScriptPath`, so timeless-jewel builds crashed the evaluator. `pob_eval.ensure_timeless_jewel_bins()` pre-inflates the LUTs to `.bin` offline (Python zlib) + harness overrides point the loader at the `.bin` — the ladder 84M Cyclone now evals (CombinedDPS ≈109M). Local tooling only, no user-facing change. **LUT god-seed search SHIPPED (Step 63, 2026-05-25):** `optimize_timeless` ranks jewel sockets by in-radius allocated notables, BFS-paths to the best, scans the Lethal Pride seed range in Lua (`data.readLUT`→`tree.legion.additions`), scores by build keywords, full-evals the top seed across conquerors, keeps the best (fitness-gated). `encode_pob_code` gained a `jewels` param (Item + Socket). Integrated into precompute → Cyclone 110k→120k, Vortex 152k→158k, etc. User-facing. 773 tests / 145 mypy. **Still open: Glorious Vanity (node replacement) + the other jewel types; node-replacement/conqueror-attribute scoring; multi-jewel.** Resolved: "Large" radius is index 3 (`jewelRadius[i].label`), conquerors Kaom/Rakiata/Akoya (additions are seed-only, conqueror adds a flat attribute). **(6) auras / flasks / Pantheon** (Mageblood flask stacking, Malevolence/Determination), then **large-scale precompute** of the optimised matrix.
  - **Compute reality:** at ~280 ms/eval the full mirror search space is astronomical → handled by smart search (not brute force) + offline precompute (Render serves JSON only) + ladder-as-signal to prune. "Fully autonomous mirror-tier" is a frontier, multi-month effort; each phase is already a large jump over today's 12-19%.

  - **STATUS (2026-05-25): all 4 phases have shipped their first lever.** Precomputed builds went from 12-19% of the ladder to genuinely strong: Cyclone 120k DPS / 19.5k EHP, Vortex 158k / 29k, Lacerate 99k / 21k, Arc 67k / 42k, Ice Shot 35k / 19k — each with uniques + layered defences + a LUT-found Lethal Pride.

  - **NEXT STEPS (detailed, priority order).** Each is self-contained; all are PoB-fitness-gated so they can't regress a build.
    1. **Glorious Vanity — DONE (Step 65, 2026-05-25).** GV added to `optimize_timeless`: replacement-aware Lua search (`readLUT(seed,node,1)` >= 96 → `tree.legion.nodes[v+1-96].sd`, else addition), conquerors Doryani/Xibaqua/Ahuana each tried (they grant Corrupted Soul / Divine Flesh / Immortal Ambition). The optimiser picked GV (Doryani) for Cyclone/Lacerate and Heroic Tragedy for the rest: Cyclone 119.5k→128.5k, Lacerate 98.6k→108.3k (+10%), Vortex 165k→180k (+9%), Arc 68.9k→82.5k (+20%), Ice Shot 37.8k→43.7k (+16%). All PoB-validated, fitness-gated. 773 tests / 145 mypy.
    2. **Other timeless jewels — DONE (Step 64, 2026-05-25).** Parametrised `optimize_timeless` to a `_TIMELESS_TYPES` table; the search now tries Lethal Pride / Brutal Restraint / Militant Faith / Heroic Tragedy per socket and keeps the PoB-fitness best type. The optimiser autonomously preferred **Brutal Restraint** on 4/5 builds: Vortex 158k→165k (+5%), Ice Shot 35.4k→37.8k (+7%), Arc +3%. (Elegant Hubris type 5 deferred — its readLUT divides the seed by 20, a special case.) 773 tests / 145 mypy.
    3. **Multi-mod / influenced / meta-crafted rares — FIRST SLICE DONE (Step 69, 2026-05-29).** Rare-body gem-level influence affixes ("+1 to Level of Socketed Skill Gems" Shaper/Crusader, "+1 to Level of Socketed Support Gems" Elder/Redeemer) — the primary 6L is socketed in the Body Armour, so these directly scale the main skill. Real RePoE mods (`local_socketed_active_skill_gem_level_+`/`local_socketed_support_gem_level_+`), influence-aware spawn in `realmods`, ilvl-80 (endgame only), led in `_stat_priorities` for the body. Verified +6% in isolation; precompute Vortex +16% (its body is live). **Discovered:** Bringer-of-Rain builds ("Can't use Chest armour") void the body → the main 6L should be socketed in the *helmet* (uses Bringer's 3 free level-30 supports + the void body otherwise wastes the gem-level mod) → **new NEXT STEP below**. Still open: broader multi-mod *composer* for the non-body rare slots (gloves/boots/rings: crit-multi + life + influence combos), needs extending `extract_mods.py` to crafted/fractured pools + a per-slot composer the optimiser scores.
    4. **Auras / flasks / Pantheon (multiplicative buffs).** **Part 1 DONE (Step 66, 2026-05-25):** `optimize_auras` forward-selects auras/heralds + a **reservation honesty gate**. **Part 2 DONE (Step 67, 2026-05-29):** `GemLink.extra_actives` + `_to_pob_gems` express a **multi-active aura group + Enlighten** (Enlighten linked to all auras → its reservation efficiency applies to each); `optimize_auras` replaces the base layout's wasteful single-aura group with one proper group, forward-selects runnable auras, and `bfs_path`s in generic reservation-efficiency notables (`_RES_EFF_NODES`: Charisma/Sovereignty/Champion of the Cause/Leader of the Pack — **PoB only applies a node's stats when connected**) bounded by `max_res_points`. Auras run LAST (final tree+uniques+jewels → real mana pool); gate tightened to strict 0. Result: Cyclone 128.5k→173.3k, Lacerate→148.3k, Vortex→241.2k, Arc→123.6k, Ice Shot→51.4k (+18-50%), all reservation-positive + res capped. **Still open: flask effects + a Pantheon (`<Build pantheonMajorGod/minorGod>`).**
    5. **Honest tree point budget — DONE (Step 68, 2026-05-29).** `trim_to_budget` (`optimize_build.py`) drops the lowest-`_score_node` removable leaves (connectivity-preserving; protects the start, jewel socket, pathed reservation notables) until the build fits `_TREE_POINT_BUDGET = 123` (level-100: 99 + 22 quest + 2 bandit). Builds now encode at level 100 (`_CHAR_LEVEL`); wired as pass #7 after auras. Arc was at 144 points (impossible) → all served builds now exactly 123 (115 regular + 8 masteries). Honest cost: −8 to −11% DPS on the 3 over-budget builds (Vortex/Arc/Ice Shot); Cyclone/Lacerate unchanged. Test: `test_precomputed_builds_fit_point_budget`.
    6. **Expand the precompute matrix beyond 5 archetypes.** Add popular builds (RF Chieftain/Jugg, Spark, Boneshatter, Tornado Shot, Poison BV, Spectre/SRS minions, Penance Brand, …). Each is a matrix row in `precompute_builds.py`; re-run is offline. Grows coverage so more `/theorycrafter` requests hit a precomputed optimum instead of the live estimate.
    7. **Conqueror-attribute optimisation (minor).** `optimize_timeless` already evals all 3 Lethal Pride conquerors but they tied for Cyclone (flat attribute didn't matter); for attribute-stacking builds (e.g. Str-stackers) the conqueror choice matters — score the conqueror's flat-attribute bonus too.
    8. **Spectre DPS proper (the last sweep LOW_DPS).** Raise Spectre reports 0 until a spectre monster is selected; needs vendored monster data + encoding a default meta spectre into the export. Currently flagged with a viability warning (Step 55). Separate from the optimiser.
    9. **Chest-forbidding-helmet socketing — DONE (Step 70, 2026-05-29).** `_forbids_chest` + `_relocate_no_chest` (`optimize_build.py`) move the primary 6L Body Armour → Helmet on a chest-forbidding helmet (The Bringer of Rain), applied as a **final overlay** in `precompute_builds.py` (after the trim), NOT inside the greedy: relocating *during* `optimize_uniques` undervalued such helmets (dropping the body's defences early → Cyclone wrongly picked Ahn's Contempt 118k); relocating *before* the tree/aura passes let them re-path worse (Cyclone 143k). Minimal — only the primary moves (PoB's FullDPS sums every `includeInFullDPS` group, so dropping the utility 4L would have *lowered* FullDPS ~40k). Unequippable body dropped from the served gear. Result: Lacerate 143.6k→**177.7k (+24%)**, Cyclone 173.3k→**179.4k (+3.5%)** — now socket the 6L honestly in the Bringer (was a phantom body → a "niente fittizio" fix); Vortex/Arc/Ice Shot unchanged (Arc picked Fractal Thoughts → live body + Step-69 gem-level mods, never wanted Bringer). Test: `test_chest_forbidding_helmet_sockets_primary_in_helmet`. **Residual:** the optimiser could still pick Bringer for a spell build where its melee supports are wasted (Arc happened to pick a better helmet this run); a future refinement would re-evaluate the helmet on the *final* build excluding/relocating chest-forbidders. Low priority.

  - **Process notes for whoever picks this up:** (a) the optimiser/precompute is **local-only** (needs `.pob_runtime/` via `scripts/setup_pob.py`); Render never runs PoB, it serves `data/theory/precomputed_3_28.json`. (b) `pob_eval.PobEvaluator` chdir's into PoB's `src/`, so any output path must be repo-root-anchored (a relative path silently lands under `.pob_runtime/src/`). (c) Validate every change with `scripts/compare_ladder.py <Ascendancy> <Skill>` (DoT-aware, uses CombinedDPS) and re-run `scripts/precompute_builds.py` to refresh the vendored optima + bump the Patch Notes when DPS/EHP move. (d) the pre-commit hook normalises line endings → if a commit aborts, just `git add -u` and re-commit.

### NEEDS DESIGN / FOLLOW-UP

- _(none — Theorycrafter notable-efficiency shipped in Step 49)_

### CANDIDATE FUTURE WORK

- **Theorycrafter — Item & Modifier Browser (full version)** — affix pools + numeric ranges; needs the slimmed RePoE mods vendor file. Deferred.
- **Theorycrafter — Item Filter Generator** — standalone tool, separate from Build Generator. Deferred.
- **Theorycrafter — Atlas Strategy Generator** + curated scarab table — standalone tool. Deferred.
- **Build Generator — LLM rationale layer** — future optional enhancement (per-call cost). Only for text explanation, never for data generation.
- **Chatbot in-app** — conversational PoE assistant. Approach TBD.

### DONE

- [x] **Step 70 — Theorycrafter: socketing per elmi che vietano il pettorale** (2026-05-29) — l'ottimizzatore sceglie The Bringer of Rain (elmo) sui build melee/bow; il suo mod "Can't use Chest armour" fa **annullare il body** a PoB, ma l'encoder socketava la 6L in quel body inesistente → **fittizio** (in-game non hai un body in cui socketare) e i 3 supporti gratis del Bringer restavano inutilizzati. `_forbids_chest`/`_relocate_no_chest` spostano la 6L nell'elmo (unico socketing legale, e prende i supporti del Bringer), applicato come **overlay finale** dopo il trim (NON dentro il greedy: la rilocazione durante `optimize_uniques` sottovalutava l'elmo, prima di tree/aure faceva re-path peggiore). Minimale: solo la 6L si sposta (FullDPS somma tutti i gruppi → droppare la 4L utility avrebbe tolto ~40k). Body inservibile rimosso dal display. Risultato: Lacerate 143.6k→**177.7k (+24%)**, Cyclone 173.3k→**179.4k (+3.5%)** — ora onesti (6L nel Bringer); Vortex/Arc/Ice Shot invariati (Arc ha scelto Fractal Thoughts → body vivo). 777 tests / 145 mypy.
- [x] **Step 69 — Theorycrafter: mod gem-level influence sul body rare** (2026-05-29) — prima fetta del #3 (rare multi-mod). La 6L principale è socketata nel Body Armour → i suoi affissi mirror sono i gem-level influence: "+1 to Level of Socketed Skill Gems" (Shaper/Crusader) alza la skill principale, "+1 to Level of Socketed Support Gems" (Elder/Redeemer) alza i supporti. Mod RePoE reali (`local_socketed_active_skill_gem_level_+`/`_support_`), spawn influence-aware in `realmods` (i mod spawnano solo via tag influence → si augmentano i tag del body), ilvl-80 (solo endgame). Verificato +6% in isolamento; precompute Vortex +16% (body vivo, elmo Galesight). **Scoperto:** i build con The Bringer of Rain ("Can't use Chest armour") annullano il body → la 6L va socketata nell'elmo (usa i 3 supporti gratis del Bringer) → nuovo NEXT STEP #9. Beneficio principale: il generatore live (body rari, mai Bringer). 776 tests / 145 mypy.
- [x] **Step 68 — Theorycrafter: budget punti onesto** (2026-05-29) — i pass timeless (Step 63) + aure (Step 67) spingevano l'allocazione oltre un budget realistico (Arc a **144 punti**, impossibile da allocare). Una build da 144 punti è fittizia. `trim_to_budget` (`optimize_build.py`) droppa le foglie removibili a `_score_node` più basso (preservando la connettività; protegge start, socket jewel, notabili di riserva pathati) finché la build rientra in `_TREE_POINT_BUDGET = 123` (livello 100: 99 + 22 quest + 2 bandit). Le build ora si codificano a livello 100 (`_CHAR_LEVEL`); trim come pass #7 dopo le aure. Tutte le build servite ora a esattamente 123 punti (115 regolari + 8 maestrie). Costo onesto: −8/−11% DPS sulle 3 over-budget (Vortex/Arc/Ice Shot); Cyclone/Lacerate invariate. 775 tests / 145 mypy.
- [x] **Step 67 — Theorycrafter: efficienza di riserva → vere multi-aura** (2026-05-29) — Step 66 aveva scoperto che i build erano saturi di riserva con UNA sola aura (per giunta l'aura base Gloves Hatred+Generosity, che su un build Brutality non buffa nessuno) e il gate di onestà aggiungeva 0. Step 67 sblocca l'efficienza di riserva: `GemLink.extra_actives` + `_to_pob_gems` esprimono un **gruppo multi-aura + Enlighten** (Enlighten linkato a tutte le aure → la sua efficienza si applica a ciascuna); `optimize_auras` sostituisce il gruppo-aura base inutile con uno vero, forward-seleziona le aure runnable e **patha** notabili generici di efficienza di riserva (`_RES_EFF_NODES`: Charisma/Sovereignty/Champion of the Cause/Leader of the Pack — scoperta decisiva: **PoB applica le stat di un nodo solo se connesso**) entro `max_res_points`. Le aure girano ULTIME nella pipeline (sul build finale tree+uniques+jewels → mana pool reale); gate stretto a 0 stretto. Misurato (PoB-esatto): Cyclone 128.5k→173.3k, Lacerate→148.3k, Vortex→241.2k, Arc→123.6k, Ice Shot→51.4k (+18-50%), tutti reservation-positive + res cappate. `_gem_level` cappa Empower/Enhance/Enlighten regolari a 4. 774 tests / 145 mypy.
- [x] **Step 57 — Theorycrafter: niente keystone auto-allocati** (2026-05-25) — i fallimenti difensivi residui (Frost Blades 1 vita, Lightning Trap 0 ES, Summon Skeletons pool basso) avevano un'unica radice: lo scorer a keyword allocava keystone che spaccano la build (Chaos Inoculation su build vita → vita 1; Avatar of Fire + rework ES su trapper ES → ES 0). Fix: `_keystone_ids(td)` unito al set `excluded` del generatore live → keystone esclusi da target/travel/fill. L'ottimizzatore PoB (Step 56) può ancora aggiungerli (validati dalla fitness reale), quindi le build precomputate tengono quelli buoni. **LOW_POOL 3 → 0, OK 23 → 26**; alzati anche i DPS di build che prendevano keystone dannosi (Cyclone 16.8k→32.9k, Arc 25.9k→41.3k). 766 tests / 141 mypy.
- [x] **Step 56 — Theorycrafter build precomputate ottimizzate con PoB** (2026-05-25) — pipeline completa: co-ottimizzazione gear (`optimize_weapon` prova le migliori basi arma della classe per fitness PoB; l'ottimizzatore ora regola supporti + arma + albero) → precompute offline (`scripts/precompute_builds.py` su 5 archetipi → `data/theory/precomputed_3_28.json`, ~166 KB) → serving live (`theory.precomputed.lookup` + `POST /fob/theory/generate` preferisce l'ottimo, fallback al generatore). `StatEstimate` con `full_dps`/`total_ehp`, `BuildSkeleton` con `optimised`; UI con badge verde "Ottimizzato con PoB" + DPS/EHP reali. Render non esegue mai PoB (serve solo il JSON vendorizzato, deploy $0). Misurato: Cyclone 16.8k→22.6k, Vortex 92.8k→124.7k FullDPS. 765 tests / 141 mypy.
- [x] **Step 55 — Theorycrafter DPS dei minion** (2026-05-25) — due bug: (1) le gemme che evocano minion hanno tag da caster, quindi `_CORE_SUPPORTS` socketava Spell Echo/Conc Effect/Unleash (inutili per i minion) prima dei supporti `createsminion` → aggiunto `_MINION_SUPPORTS` + branch minion in `_select_supports_raw` (Minion Damage/Feeding Frenzy/Predator/… primi); (2) `_DAMAGE_KEYWORDS` non aveva una voce minion → 0 nodi minion sull'albero → aggiunti i keyword `"minion"` e instradate le skill minion lì. Summon Skeletons 515 → 4327 FullDPS. Raise Spectre resta 0 (PoB richiede uno *spettro selezionato*; impossibile senza dati monster vendorizzati) → aggiunto warning di viability `spectre_needs_selection`. **LOW_DPS 2 → 1.** 761 tests / 138 mypy.
- [x] **Step 54 — Theorycrafter pool ES + distribuzione resistenze** (2026-05-25) — `_DEFENCE_TAG["es"]` era `"energy_shield"`, un tag che **nessuna base** del catalogo possiede → `_pick_base` ripiegava sulla base col drop-level più alto (ibrida) su cui `local_energy_shield_+%` (solo `int_armour`) non può rollare → ES assente da elmo/guanti/stivali → pool ~3k. Corretto a `"int_armour"`. Ribilanciata la distribuzione delle resistenze in `_stat_priorities` (Fire era su 7 slot, Lightning su 1 → ogni res elementale ora su ~4-5 slot). Misurato (PoB-reale): sei caster ES 3.0-3.1k → 4.4-4.7k ES, tutte le res cappate; **OK 16 → 23, LOW_RES 3 → 0, LOW_POOL 6 → 2** (i 2 rimasti fuori scope/pre-esistenti: una build *vita* su Shadow a 744 vita, e un quirk ES=0 su Saboteur). 759 tests / 138 mypy.
- [x] **Step 53 — Theorycrafter classificazione attacchi elementali** (2026-05-25) — `_stat_priorities(slot, intent, skill)` decide attacco-vs-incantesimo dai **tag** della skill (`is_attack = "attack" in skill.tags`), non dal tipo di danno. Gli attacchi (fisici O elementali) ricevono stat da attacco + `Adds <Elemento> Damage` sull'arma/gioielleria; gli incantesimi mantengono le stat da caster. `_is_bow_skill` (euristica `miragearchercanuse`+`rangedattack`, niente `melee`/`wandattack`/`spell`) instrada Ice Shot su un Arco (PoB non tagga gli archi con un SkillType `bow`). Spectral Throw (attacco ranged ad arma melee) ora prende un'arma melee, non una Wand. `_verdict` in qa_sweep reso onesto (ispeziona l'arma generata / la presenza di stat da spell, non un check statico sul tipo di danno). Sweep: WRONG_WEAPON 4 → 0, OK 13 → 16, nessuna regressione. 758 tests / 138 mypy.
- [x] **Step 49 — Theorycrafter ottimizzazione albero (valore-per-punto)** (2026-05-22, QA Riccardo) — nuovo `_grow_to_value`: greedy che alloca il notable/keystone col miglior rapporto `score / punti-per-raggiungerlo` (BFS multi-source) + il travel di collegamento, sostituendo il vecchio waypoint-walk + boundary-fill. Scorer arricchito (`_SURVIVAL_WEIGHTS`): res max, suppression, attributi, block, vita/ES contano per ogni build → preferisce "+2% all max res" (1 punto) a due nodi res singola. Score precalcolati una volta (perf: 67s→~1s/build). Da ~16 notable + ~76 travel inutile a ~28-31 notable + 8 maestrie, 1 componente connesso, localizzato, weapon-correct. 756 tests / 132 mypy.
- [x] **Step 48 — Theorycrafter maestrie + nodi puliti + stime oneste** (2026-05-22, QA Riccardo) — il tree loader tiene i `masteryEffects`; `_select_masteries` alloca fino a 8 maestrie (effetto migliore per la build) sulle ruote prese; `_excluded_weapon_ids` esclude i nodi per armi non usate (no "danno con le asce" su build con spada); `_rollable_priorities` filtra le stat per slot anche a front-end (no danno fisico sull'elmo) con candidati locali/globali (ES, attack speed, accuracy); stima Vita realistica (~5k, era ~13k), DPS index rimosso (fuorviante). `mods_3_28.json` rigenerato (21 stat / 325 tier). Rimane non ottimizzata l'efficienza dei notable (es. +2 all max res vs due +1). 756 tests / 132 mypy.
- [x] **Step 47 — Theorycrafter modificatori reali (RePoE)** (2026-05-22, QA Riccardo) — nuovo `data/mods/mods_3_28.json` (~40 KB, da `scripts/extract_mods.py` su RePoE `mods.json` + `stat_translations.json`, filtrato ai ~18 stat che raccomandiamo); nuovo modulo `theory.realmods` con `real_affix_line` (stem→stat id reale, filtro spawn-weight reale per slot, tier migliore entro il budget, valore reale). `_theory_item_body` emette SOLO mod reali (priorità che non possono comparire sullo slot vengono omesse). Rimosso `_AFFIX_VALUES`/`_affix_line`/`_BUDGET_COL` (simulati). 755 tests / 132 mypy.
- [x] **Step 46 — Theorycrafter pathing albero localizzato** (2026-05-22, QA Riccardo) — penalità di distanza (`_LOCALITY_ALPHA`) sui waypoint + fill, `_regular_distances` (BFS hop sul solo albero regolare), `bfs_path` vieta nodi non-regolari (no leak mastery/cluster), rimosso lo step di connessione all'ascendancy. Da median ~24 hop (Ranger, sprawl) a ~7, 0 leak, 0 isole. Drift dati escluso (tutti i 3337 ID esistono nel tree.lua di PoB 3.28). 754 tests / 132 mypy.
- [x] **Bug — Theorycrafter gem links: support incompatibili + livello Awakened + ascendancy float** (2026-05-22, QA Riccardo) — `extract_gems.py` non scarta piu i SkillType discriminanti (Trapped/MeleeSingleTarget/Multistrikeable…), `_is_support` strict (tinctures fuori), `_select_supports_raw` con semantica PoB any-of + `_SUPPORT_DMG_LOCK` (no Brutality su build elementali), `_CORE_SUPPORTS` ordina i support meta reali, `_gem_level` cappa le Awakened a 5, `_to_pob_tree` esclude i notable ascendancy dai nodi dell'albero. 753 tests / 132 mypy.
- [x] **UX — input diretti su Finder / Analyze / Planner** (2026-05-22, QA Riccardo) — Finder: invio query → extract + recommend in un colpo solo (chain in `extractMut.onSuccess`), il bottone "Trova build" resta per il refine dei filtri. Tutti e tre i pannelli: rimosso il collapse a `<Code>` + "modifica" → input sempre editabile (helper/hint nascosti quando c'è un risultato). Frontend-only. 753 tests / 132 mypy.
- [x] **Step 45d — Build Generator: item reali con stat priority per slot** (2026-05-22, Prompt 033c) — `_stat_priorities` riscritta come mappa esplicita per slot (spell/attack, ES/life, crit), ordinata per priorità reale; `_AFFIX_VALUES` esteso (Cast Speed, Flask Life Recovery, to Mana, all Attributes, Accuracy, Chance to Block, Critical Strike Multiplier — quest'ultima PRIMA di "critical strike" per via del match-substring); `_theory_item_body` rinomina "Theorycrafted"→"Generated" e rende le flask come oggetti MAGIC `<base> <suffix>` via `_FLASK_SUFFIX`; il weapon usa `weapon_label` (Wand/Bow/Weapon). 753 tests / 132 mypy.
- [x] **Step 45c — Build Generator: Awakened gem allowlist 3.28** (2026-05-22, Prompt 033b) — 3.28 ha rimosso dalla drop pool tutte le Awakened tranne Empower/Enlighten/Enhance. `_AWAKENED_ALLOWLIST` + `_is_available_in_328` filtrano dentro `_select_supports_raw` (eredita in `_select_supports`/`_pick_supports_for`) + guardia in `_assert_valid`. **Deviazione dati**: il catalogo memorizza i nomi Awakened SENZA suffisso " Support" (`"Awakened Empower"`), quindi l'allowlist usa quei nomi esatti — le stringhe con " Support" del prompt avrebbero bloccato tutte e 38 incluse le 3 valide. 752 tests / 132 mypy.
- [x] **Step 45b — Build Generator: gem layout deduplicato + support compatibili** (2026-05-22, Prompt 033) — Bug 1: l'Helmet 4L ospitava la primary skill (duplicata col Body 6L) → nuovo `_SECONDARY_SKILL` + `_pick_secondary` scelgono una skill diversa per tag; il movement nei boots evita primary e secondary. Bug 2: i support hard-coded (es. Faster Casting su skill da attacco) passavano solo il check di esistenza → `_select_supports` splittata in `_select_supports_raw` (oggetti `_Support`, tag-filtrati) + `_pick_supports_for(skill, prefer, n)` che tiene solo i prefer compatibili e completa dal pool compatibile. Nessuna skill attiva ripetuta tra i 5 link. 751 tests / 132 mypy.
- [x] **Step 45a — Build Generator: tree node budget reale (~120 nodi)** (2026-05-22, Prompt 032) — `_select_tree_nodes` waypoint targets espansi a top-16 notable + top-4 keystone; nuovo `_fill_to_budget` greedy boundary-expansion (best-scored frontier node, ties su id minore) satura `_MAX_TREE_NODES` mantenendo il singolo componente connesso. Output classificato per flag del `TreeNode` (keystone/notable/travel). Intent Marauder/Juggernaut Cyclone → 120 path node (92 travel). `bfs_path` e `_score_node` invariati; nessun cambio frontend. 749 tests / 132 mypy.
- [x] **Step 44 — Build Generator BFS tree pathing** (2026-05-20, Prompt 031) — `bfs_path` module-level helper + visit-tracking greedy waypoint expansion in `_select_tree_nodes`. Every consecutive node pair in the result is adjacent in `TreeData.adjacency`. `TreeNodeRef.type` gains `"travel"`; frontend filters travel nodes from display + caption. 747 tests / 132 mypy.
- [x] **Step 43 — Build Generator viability validation pass** (2026-05-20, Prompt 030) — new `theory.viability` module + `ViabilityReport` attached to every `BuildSkeleton`. 6 checks: gear-side resistance reminder (always), life/ES floor per budget (errors), single defence layer, missing movement skill, missing mana sustain (warnings). Frontend `ViabilityPanel` with green/amber/red alert states. 741 tests / 131 mypy.
- [x] **Step 42 — Theorycrafter gear card UX + Trade dialog** (2026-05-20, Prompt 029) — frontend-only. Expandable gear cards with per-card `useState`, simulated affix list with sigils + `~ stimato` label, `TradeSearchDialog` opened per slot from parent state (mirrors Analyze/Planner), two-column Grid on `md+`. No backend touched. 732 tests / 129 mypy.
- [x] **Step 41 — Build Generator v2: PoB export completeness** (2026-05-20, Prompt 028) — five structural bugs fixed: tree scoring uses real `node.stats`; 5 gem groups (Body 6L + Helmet/Gloves/Boots/Weapon 4L); items ship simulated budget-scaled affix lines; 5 flask slots + 2 jewel slots; class start uses the right class index. 732 tests / 129 mypy.
- [x] **Step 40 — Theorycrafter Build Generator v2** (2026-05-20, Prompt 027) — form-driven UI, graph engine over vendored 3.28 data, complete importable PoB code, Trade icon per gear slot, anti-hallucination runtime asserts. 727 tests / 128 mypy.
- [x] **Step 39 — Theorycrafter Build Generator v1** (2026-05-19, Prompt 026) — superseded by Step 40.
- [x] **Steps 33–38r** — Visual polish, architectural reset, View Transitions. ✅
- [x] **Steps 25–32 — Trade redirect + Trade dialog** (2026-05-18). ✅
- [x] **Steps 1–24** — See `CLAUDE.md`. ✅

### REJECTED / OBSOLETE

- ~~PostgreSQL data layer~~ → diskcache + poe.ninja.
- ~~poedb.tw scraping~~ → vendored JSON.
- ~~Hand-curated PROGRESSION registries~~ → dynamic synthesis (Steps 16-19).
- ~~New BuildTemplate subclasses per skill~~ → 49 templates frozen; stage data is dynamic.
- ~~18-archetype hardcoded JSON as sole generator~~ → superseded by graph engine (Step 40).
- ~~Free-text natural language input for Build Generator~~ → form-driven UI (Step 40).
- ~~Tab shell with disabled Atlas/Loot Filter tabs on /theorycrafter~~ → removed in Step 40.
- ~~Hand-authored gem fallback JSON~~ → official PoB Community data only, no fallbacks.

---

## 7. Decision log

Reverse-chronological.

- **2026-05-25** — *Aura pass + reservation honesty (Step 66, Step-4 part 1).* Built `optimize_auras` (forward-select auras/heralds). A first run looked amazing (+40% DPS from 4 auras) — but it was a *lie*: PoB applies aura buffs even when mana is over-reserved (`ManaUnreserved` −1100/−1635), which no real build can do. Added a reservation honesty gate to `fitness` (`ManaUnreserved < −10 → ×0.1`); now the optimiser only keeps auras that genuinely fit, and every precomputed build is reservation-positive. Honest consequence: the builds already run one aura (base gem layout) that saturates their reservation, so 0 extra auras fit → no DPS change, JSON unchanged, no Patch Notes. Important *correctness* win regardless — the gate protects every pass from preferring unrunnable over-reserved candidates. The real aura value needs **reservation efficiency** (Enlighten-linked multi-aura group — blocked by `GemLink` only supporting one active gem — + reservation nodes/items), logged as the next sub-step. Lesson: PoB's calc reports buffs that aren't actually attainable; any "buff" lever (auras, and later flasks/charges) must be gated on its real cost (reservation, uptime) or the optimiser will cheat.
- **2026-05-25** — *Glorious Vanity / node-replacement timeless jewel (Step 65).* The headline mirror lever, done. GV differs from the additive jewels: `readLUT(seed,node,1)` returns a value ≥ `timelessJewelAdditions` (96) = a *replacement* node id (→ `tree.legion.nodes[v+1-96]`, a Vaal node with its own stat block), else an addition. Proved it headless first (GV replaces "Steadfast" with seed-dependent Vaal notables), then made the Lua seed-search scorer replacement-aware (works uniformly for all types) and turned `_JewelType.conquerors` into a tuple so GV's three conquerors — the build-defining keystones Corrupted Soul/Divine Flesh/Immortal Ambition — are each evaluated. The optimiser autonomously chose GV (Doryani/Corrupted Soul) for the physical melee builds (Cyclone, Lacerate) and Heroic Tragedy for the casters/bow, with big gains: Arc +20%, Ice Shot +16%, Vortex +9%, Lacerate +10%. Fitness-gated so a node-replacement that nets a loss is never kept. Note Cyclone traded a little EHP for the DPS (GV replaced some defensive notables) but stayed above floor — the fitness reward judged it worth it. With this, the timeless-jewel lever (the single biggest physical-build multiplier) is fully in the optimiser's hands across all jewel types except Elegant Hubris (÷20 seed, deferred).
- **2026-05-25** — *All additive timeless jewel types (Step 64).* Generalised Step 63's Lethal-Pride-only search to a `_TIMELESS_TYPES` table (each type's unique seed-bearing flavour line + radius-transform line + conquerors, pulled from PoB's `TreeTab.conquerorTypes` + `ModItemExclusive`). The search tries Lethal Pride / Brutal Restraint / Militant Faith / Heroic Tragedy at each ranked socket and keeps the PoB-fitness best. Finding: **Brutal Restraint** (Maraketh) beats Lethal Pride on 4/5 precomputed builds — Vortex +5%, Ice Shot +7%, Arc +3% — the optimiser chose it autonomously (no curation). Confirms the "widen the search space, PoB picks" thesis once more. Elegant Hubris (type 5) deferred for its ÷20 seed quirk; Glorious Vanity (node replacement) remains the headline next lever.
- **2026-05-25** — *Timeless Jewels SHIPPED via LUT god-seed search (Step 63).* Built the full feature ("fallo per intero"). `optimize_timeless`: rank the 60 jewel sockets by in-radius allocated-notable coverage (`nodesInRadius[3]`, the "Large" radius — `jewelRadius[i].label`), BFS-path to the best few, then scan the Lethal Pride seed range (10000-18000) **entirely in PoB's Lua state** (`data.readLUT(seed,node,2)` → `tree.legion.additions[idx+1].sd`, scored by the build's keywords), full-eval the top seed across the three conquerors, keep the best (fitness-gated). `encode_pob_code` gained a `jewels` param emitting the Item + `<Socket>`. Debug notes: the radius field is `.label` not `.name` (one wrong-empty cycle); `nodesInRadius[3]` keyed by numeric node id; only NOTABLES transform for non-GV. Measured: every precomputed build +2-9% DPS from a real LUT-found Lethal Pride. Honest scope: Lethal Pride only (additive/safe); Glorious Vanity (node replacement) + node-replacement scoring + conqueror flat-attribute optimisation are future work; minor point-budget caveat (socket+path). Closes the headline mirror-tier lever on physical builds.
- **2026-05-25** — *Timeless Jewel LUT god-seed search: feasibility PROVEN + design spec'd (no code committed yet).* Riccardo chose the "real" approach (LUT-query seed search, not sampling/curation). De-risked the keystone headless: `data.readLUT(seed, nodeID, jewelType)` works in our PoB state (Step 62 unblock) and returns, for a notable, the addition index → `build.spec.tree.legion.additions[idx+1].sd` gives the stat text. Proven on notable 13922 "Steadfast" with Lethal Pride (type 2): seed 10000→"Regen 1% Life/sec", 16000→"5% incr Strength", 18000→"0.4% Attack Dmg Leeched", etc. **Full design (spec for the build):** (1) only NOTABLE nodes transform for non-GV jewels (`index <= nodeIDList.sizeNotable=452`); (2) a socket's in-radius nodes are `tree.nodes[socket].nodesInRadius[radiusIdx]` (need the "Large"/timeless radius index — `nodesInRadius` has indices 1-10 with varying counts; identify the timeless-jewel radius); (3) seed ranges from `data.timelessJewelSeedMin/Max` (Lethal Pride 10000-18000, GV 100-8000, …); (4) search = for a socket, scan seeds, sum keyword-scored additions over in-radius notables (all in Lua, ~120k cheap readLUT calls ≈ 1-2s), take top-K seeds, then full-PoB-eval them, keep best (fitness-gated, so it never hurts the build); (5) emit the jewel `<Item>` (`Rarity: UNIQUE\n<type>\nTimeless Jewel\n…\nCommanded leadership over <seed> warriors under <conqueror>\n…`) + a `<Socket nodeId=<socket> itemId=<id>/>` in `<Spec>` (XML post-injection); (6) the socket node must be ALLOCATED (path to it). **Still to resolve in the build:** the conqueror data structure (the probe's `legion.conquerors` guess was wrong — Lethal Pride conquerors Kaom/Rakiata/Akoya also grant a flat attribute to all in-radius nodes; find the right table) and the exact "Large" radius index. **Honest call:** the keystone (headless LUT query) is proven; the full feature (radius + socket allocation + multi-conqueror emit + search + encoder injection + optimiser/precompute integration) is a large multi-step build deferred to a dedicated focused effort rather than risk a broken half-implementation at the tail of a long session. Throwaway probes not committed.
- **2026-05-25** — *Timeless Jewel eval unblocked (Step 62, Phase 4 prereq).* The 84M-DPS ladder Cyclone (the user's explicit target) crashed the evaluator — diagnosed to three stubs in PoB's `HeadlessWrapper.lua`: `Inflate` returns "" (can't decompress the `.zip` LUTs), `NewFileSearch` returns nil (never finds the `.bin`), `GetScriptPath` returns "" (bogus absolute paths). Rather than implement a runtime Inflate (the data is a raw zlib stream; LuaJIT-FFI streaming inflate is fiddly because the output size is unknown), the cleaner path: pre-inflate the LUTs to `.bin` **offline** with Python zlib (idempotent, in `pob_eval.__init__`) + override `GetScriptPath`→"." and a minimal `NewFileSearch` that reports the `.bin` as up-to-date (huge mtime) so PoB's loader reads it directly via `io.open` — no runtime Inflate. Scoped the `NewFileSearch` override to `*.bin` only (nil elsewhere, preserving the stub). Verified: the ladder Cyclone now evals to ~109M CombinedDPS. This is infrastructure (the prerequisite for jewel optimisation) — it doesn't change our generated builds yet, so no Patch Notes (Step 50 precedent). Honest note: the *big* win (adding timeless/cluster jewels to the optimiser's search space — pick type/socket/seed) is the next, larger step; this just makes it possible to evaluate them.
- **2026-05-25** — *DoT-multiplier gear (Step 61, Phase 3 part 1).* The Vortex ladder gap pointed at non-hit damage: our gear damage is flat-added (hit) only, doing ~0 for a DoT build. Probed RePoE for the DoT-multiplier stat ids — finding that only the *generic* `dot_multiplier_+` rolls as a normal affix (bow/1H/amulet); the element-specific ones (`cold_dot_multiplier_+` etc.) have empty spawn weights, i.e. they come from essences/influence/uniques, not regular crafting. So the generator now leads DoT builds' weapon+amulet with the generic "Damage over Time Multiplier" and drops the dead flat-added-to-spells; element DoT scaling flows through the optimiser's uniques pass (Rime Gaze). **Honest finding:** this *doesn't* move the precomputed builds — Vortex's amulet+weapon become uniques (overwriting the rare DoT mods) and its FullDPS already exceeds CombinedDPS (so fitness already credits the DoT). The value is correctness for the **live generator's** DoT recommendations. Decided to ship it as a focused, tested correctness fix rather than overclaim a precompute gain. The bigger remaining gear lever for precomputed builds is multi-mod/meta-crafted rares (item 7) — flagged for next.
- **2026-05-25** — *Layered-EHP fitness (Step 60, Phase 2).* The ladder comparison's other half: our builds had DPS at the pool floor and ~1.3× EHP-over-pool vs the ladder's ~10×. Insight: the defensive layers (block/suppression/MoM/CI, defensive uniques) were *already* in the optimiser's search space — the gap was purely the fitness function not rewarding EHP. One-function change: a sublinear saturating EHP bonus (`0.4 + 0.6·min(EHP/target,1)^0.5`). Chose sublinear+saturating so DPS still leads (linear) and the optimiser takes *cheap* EHP (a block shield, Aegis Aurora) without throwing away DPS for marginal defence — matching how real builds layer mostly-free mitigation. Measured: Arc nearly doubled EHP (21→40k) for −3% DPS, Vortex +18% EHP for −12% DPS; already-tanky Cyclone/Lacerate unchanged (the bonus saturated). This validates the broader thesis again — the engine + search space were right; the objective just needed to express "viable DPS" as DPS×viability×EHP rather than DPS gated on a floor. Next: Phase 3 (DoT/ailment multipliers + multi-mod rares) or unblock timeless jewels.
- **2026-05-25** — *Uniques wired into the optimiser (Step 59).* Phase 1(1b) — the first big mirror-tier payoff. Key enabler was cheap: the encoder already emits a multi-line `item_name` verbatim (Step 41's `rare_craft` path), so a unique just needs its PoB text built (`unique_pob_body`, with `(min-max)`→max roll) — no encoder change. Verified empirically before building the full pass (Rime Gaze on a Vortex → PoB applied the cold-DoT multi, +12%). `optimize_uniques` then does per-slot greedy: relevance-preselect (keyword score over the unique's mods) → try top-8 → keep PoB-fitness best vs the current rare; weapon slot filtered to the build's weapon class. Threaded a `pob_gear` override through `_Encoder.code`/`optimize_tree`. Re-ran precompute: Cyclone 22.6k→110k, Vortex 124.7k→173k (EHP 6.1k→22.9k), etc. — the optimiser found the genuine chase uniques (The Bringer of Rain, Starforge, Mark of the Shaper, Galesight) on its own, PoB-validated, zero curation. This is exactly the "widen the search space, let PoB fitness decide" thesis paying off. Gotcha avoided: the display-gear overlay used a label→ItemSlot map with a clean helper (an earlier walrus version defaulted unmapped slots to HELMET — a flask could have inherited a helmet unique). Next: Phase 2 (defensive layering) or unblock timeless jewels.
- **2026-05-25** — *Mirror-tier initiative kickoff + uniques DB (Step 58).* Riccardo set the north star: generate complete mirror-tier builds from scratch (the 84M-DPS ladder Cyclone is the target). Built `scripts/compare_ladder.py` to quantify the gap against a real top build — Vortex Occultist came out at 19% of the ladder's DPS and 12% of its EHP, with the root causes being (a) zero uniques vs 6 chase uniques, (b) our gear damage is flat-added/hit-only so it does ~0 for a DoT build, (c) no defensive layering (ladder ~10× mitigation over ES, ours ~1.3×). Decision: the PoB-exact optimiser is the right engine; the work is widening its search space + feeding it compute, across a 4-phase / 7-item roadmap (logged in §6). Started Phase 1 with the biggest, most self-contained lever: vendoring the unique catalogue. `extract_uniques.py` parses PoB's `Data/Uniques/*.lua` (text blocks with `{variant:N}` tags) → 1254 uniques; took the *current* (highest-index) variant's mods, kept value ranges as text (resolution to max-roll is the encode step). Note hit on the timeless-jewel front: headless PoB can't currently load `Data/TimelessJewelData/*.zip` (the Cyclone 84M eval crashed) — deferred to Phase 1(2). This step is internal foundation (DB + loader, no wiring yet) so no Patch Notes, matching the Step 50/51 precedent. Ladder used strictly as a structure/popularity signal — Finder/Theorycrafter boundary intact. Next: wire uniques into the optimiser (try-per-slot, PoB-validated).
- **2026-05-25** — *No auto-allocated keystones (Step 57).* PoB-headless diagnosis of the three remaining LOW_POOL builds found one shared root cause: the keyword tree scorer treats keystones like notables and allocates them, but keystones are binary build-definers it can't reason about — Chaos Inoculation landed on a *life* Frost Blades (Life → 1), and Avatar of Fire + ES-rework keystones on a *lightning* ES trapper (ES → 0, wrong element). Decision: the live generator excludes ALL keystones (union `_keystone_ids` into `excluded`) rather than curating an allow/deny list — a keyword scorer can't safely pick keystones, and a build without them is strictly safer; notables carry the scaling. Crucially the **optimiser keeps keystone access** (its `excluded` is weapon-only and its swaps are PoB-fitness-gated), so precomputed builds still get the genuinely-good ones. The payoff exceeded the two target bugs: removing harmful keystones also lifted DPS broadly (Cyclone +95%, Arc +59% — they'd been getting Resolute Technique / wrong-element conversions). Cost: two ele-spell builds lost Elemental Overload (Vortex/Fireball −20% DPS) but stay strongly OK. Net sweep: OK 23→26, LOW_POOL 3→0, only Raise Spectre's spectre-monster limitation left. Bumped the locality test's hop bound 20→26 (no nearby keystone to satisfy a notable → the greedy reaches a little further). User-facing (Patch Notes).
- **2026-05-25** — *Precomputed PoB-optimised builds (Step 56, user chose "full pipeline").* The payoff of the optimiser initiative (Steps 50-52). Three sub-parts, all shipped. (1) Gear co-opt: added `optimize_weapon` — the weapon is the #1 DPS lever, so trying the top bases of the resolved class and keeping the PoB-fitness best is the highest-value gear move; `_Encoder` got a `gear=` override threaded through `code()`/`optimize_tree()`. (2) Precompute: a 5-archetype matrix script writing a vendored JSON of serialised optimised `BuildSkeleton`s. (3) Serving: `lookup(intent)` (exact 7-field match) preferred by the endpoint, live-generation fallback. Two gotchas hit and fixed: an Edit accidentally inserted `gear_with_weapon` mid-`__init__`, orphaning `self._asc` after a `return` → every PoB eval raised inside the swallowing `try/except` (fit=-1 everywhere) until fixed; and the relative output path wrote under `.pob_runtime/src/` because `PobEvaluator` chdir's into PoB's `src/` — anchored it at the repo root via `__file__`. Architectural note for §4.2: this adds a new vendored data file + changes the `/fob/theory/generate` behaviour (serve precomputed vs generate) but **no new endpoint and no Render compute** — PoB runs only locally during precompute; Render serves the JSON. $0 deploy intact. Measured gains over the Step 52-55 generator: Cyclone 16.8k→22.6k, Vortex 92.8k→124.7k FullDPS, all viable (res capped, pool floor cleared via the fitness penalty). Matrix kept at 5 to bound the offline run; expanding it is a follow-up.
- **2026-05-25** — *Minion DPS (Step 55).* The sweep's last DPS cluster (two minion builds at ~0 DPS) split into two real bugs and one PoB limitation. PoB-headless diagnosis: minion-summon gems carry `spell`/`multicastable`/`minionscanexplode`, so caster supports were *socketable* and `_CORE_SUPPORTS` ranked them first — the minion got Spell Echo + Conc Effect (zero minion benefit) instead of Minion Damage. Fixed with `_MINION_SUPPORTS` + a `createsminion`-first sort branch. Second bug: the tree scorer had no minion keyword set, so a `physical` minion build matched "physical damage" against minion notables (which say "Minions deal …") → 0 minion nodes → the minion did no bonus damage; added `_DAMAGE_KEYWORDS["minion"]` and routed minion skills there. Result: Summon Skeletons 515 → 4327 FullDPS. Judgement call on Raise Spectre: it stays 0 because PoB needs a *specific spectre monster* selected and we don't vendor monster data — rather than fake it, I added a `spectre_needs_selection` viability warning so the user knows the one manual step (the supports + tree are correct, so the scaling applies the instant a spectre is picked). Surfaced (not regressed) a separate issue: minion builds have a low *character* pool (~1900 ES — was already ~2031, masked by the LOW_DPS verdict), which is largely by-design (minions are the defensive layer) — logged as a follow-up, not force-fixed (boosting character ES would cut the minion DPS just restored). User-facing (Patch Notes).
- **2026-05-25** — *ES pool + resistance spread (Step 54).* The sweep's two defensive clusters (ES ~3k LOW_POOL, one res under-cap LOW_RES) turned out to be one dead gear filter plus a skewed resistance map — not the open "boost ES tree/gear" design the backlog assumed. Diagnosis with PoB-headless: `_DEFENCE_TAG["es"]` was `"energy_shield"`, which matches **zero** bases (confirmed by enumerating the catalogue), so `_pick_base`'s `pref_tag in b.tags` filter always failed and fell through to the drop-level sort → hybrid bases → `local_energy_shield_+%` (int_armour-only, per its spawn weights) couldn't roll → ES silently absent from 3 of 5 armour slots. One-line fix to `"int_armour"` took the six ES casters from ~3.0k to ~4.5k. Second finding: the resistance map was Fire-heavy (Fire ×7, Lightning ×1), so lightning under-capped on every ES build; rebalanced to ~4-5 slots each. Chose NOT to also boost the ES tree weight — the gear fix already cleared the floor and lifting ES-node scoring risks regressing the (now strong) damage. Two LOW_POOL remain but are out of scope and unchanged by this step (a Shadow *life* build; a Saboteur where PoB reports ES 0 / Life 2311 — flagged for later). Sweep: OK 16→23, LOW_RES 3→0, LOW_POOL 6→2. User-facing (Patch Notes).
- **2026-05-25** — *Elemental-attack classification (Step 53).* Acting on the QA sweep's confirmed WRONG_WEAPON cluster: `_stat_priorities` decided attack-vs-spell with `is_spell = damage ∈ {fire,cold,lightning,chaos}`, so elemental *attacks* (Lightning/Molten/Frost Strike, Ice Shot) got caster stats and Ice Shot got a Wand. Fixed by classifying on skill **tags** (`is_attack = "attack" in skill.tags`); `_stat_priorities` now takes the resolved `_Active`. Two judgement calls beyond the prompt sketch: (1) **bow detection** — PoB's data tags no bow attack with a `bow` SkillType (confirmed: Ice Shot/Tornado Shot/Caustic Arrow all lack it), so rather than re-vendoring the catalogue to extract `weaponTypes`, I used a documented tag heuristic `_is_bow_skill` (`miragearchercanuse`+`rangedattack`, no `melee`/`wandattack`/`spell`) — `miragearchercanuse` is bow-only (Mirage Archer), which cleanly separates Ice Shot (bow) from Power Siphon (wand) and Spectral Throw (melee weapon). (2) The honest `_verdict` then surfaced **Spectral Throw** as a real wrong-weapon (a wand on a melee-weapon attack) — not in the prompt's 4-row scope but a genuine pre-existing bug the new heuristic correctly catches; fixed it with the same principle (an `attack` skill with no `wandattack`/`bow`/`melee` → melee weapon). Kept physical-attack Accuracy on the weapon to avoid regressing `test_stat_priorities_are_slot_aware`. Measured (PoB-exact): all 4 elemental attacks correct + real DPS, WRONG_WEAPON 4→0, OK 13→16, no regressions. User-facing (Patch Notes).
- **2026-05-24** — *Gear flat added-damage mods (Step 52).* Acting on the Step 51 finding (the DPS ceiling is gear, not tree/gems): the generated gear omitted flat added-damage mods — the single biggest weapon/jewellery DPS source. These are 2-stat mods (`*_minimum_added_*` + `*_maximum_added_*`), which the Step 47 extraction skipped (it kept only single-stat mods). Extended `extract_mods.py` with an `added` section keyed by `<prefix>_<element>` (local/attack/spell × phys/fire/cold/lightning/chaos) carrying the 2-value render template; `realmods._added_line` resolves the new stems with the same spawn-weight gating; `_stat_priorities` leads the weapon with the flat-added stem (physical for attacks, element-to-spells for casters) and adds the attack variant to jewellery/gloves. Verified via PoB-headless: a Marauder/Jugg Cyclone went FullDPS 7274 → 16283 (+124 %), TotalDPS 3329 → 9151. First user-facing win of the PoB-optimiser initiative; the optimiser itself (Step 51) stays a local tool. Next: gear co-optimisation, then precompute + serve.
- **2026-05-24** — *PoB-exact optimiser (Step 51).* Built the optimiser on the Step 50 evaluator: fitness = `FullDPS × viability_penalty` (uncapped res / sub-floor pool multiply the score down → viable DPS, not glass cannon); `_Encoder` reuses fixed gear so each candidate is one PoB eval; two passes — 6L support forward-selection + connectivity-preserving tree-swap local search. **The instructive result:** the optimiser found a real 6L win (+12 % FullDPS, +65 % TotalDPS on Cyclone — Faster Attacks+Concentrated Effect over Impale+Pulverise, PoB-confirmed) but the **tree was already at a PoB local optimum** (the Step 49 greedy is genuinely good) and our heuristic supports were near-optimal too. So the headline "low DPS" the user saw is **gear-bound**: our gear recs omit flat added-damage mods (`Adds # to # …`) and crit — the dominant weapon-DPS sources. Re-prioritised the roadmap: Step 52 is now GEAR (damage mods + co-opt), not the precompute pipeline. The optimiser framework is the foundation; it'll drive gear co-optimisation next. Local/offline only, no Patch Notes.
- **2026-05-24** — *PoB-exact evaluation: spike + foundation (Step 50).* Riccardo's directive: the Theorycrafter must produce *viable and broken* builds from scratch → the optimiser needs a real fitness, "il meno approssimato possibile / esatto come PoB". The honest conclusion: *exactly* PoB = run PoB. Spiked three ways. lupa (embedded LuaJIT) **crashes** — PoB's native module DLLs bind to its own `lua51.dll`, incompatible with lupa's statically-linked LuaJIT (a real, instructive ABI wall). No Docker/WSL on the box. The winner: **ctypes on PoB's bundled `lua51.dll`** — every module binds to one runtime, native modules load, zero installs. Gotchas found in order: `package.path` needs `runtime/lua`, then `require('xml')` works; `lua-utf8` needs `package.cpath` to `runtime/?.dll` AND the dependent DLLs on PATH (`os.add_dll_directory`); `Modules/Main.lua` indexes a nil `arg` global (set `arg={}`); `CI=true` skips ModCache (avoids the stubbed `Inflate`). Result: feed our own `encode_pob_code` XML → `loadBuildFromXML` → read `build.calcsTab.mainOutput` → exact stats (Life 5548, FullDPS 7274 on a Marauder/Jugg Cyclone), ~280 ms/eval reusing one live state. Shipped `scripts/setup_pob.py` + `scripts/pob_eval.py` (`.pob_runtime/` gitignored). Local/offline only — deployed app untouched, so no Patch Notes. Next: the optimiser (Step 51).
- **2026-05-22** — *Theorycrafter notable-efficiency tree optimisation (Step 49, QA Riccardo).* The deferred "notable efficiency" item, done. The waypoint-walk + boundary-fill was structurally wrong: it picked the top-16 notables then filled ~90 points with whatever was adjacent (junk), and the scorer literally didn't value resistances. Replaced with a value-per-point greedy (`_grow_to_value`): multi-source BFS gives the new-point cost to reach each unallocated notable/keystone, and we take the best `score/cost` plus its travel. Travel cost is the locality term (so `_LOCALITY_ALPHA` is no longer needed for the main allocation, only the top-up fill). Added `_SURVIVAL_WEIGHTS` so max-res/suppression/attributes/block/life/ES are valued universally, with a premium on "+all max elemental res" so it beats two single-res points — exactly the user's example. Perf gotcha: re-scoring 3000+ nodes each iteration made a per-skill test take 67 s; precomputing the candidate scores once dropped it to ~1 s/build. NOT a full optimum (greedy, not exhaustive Steiner) but a large, real quality jump (~16→~30 meaningful notables).
- **2026-05-22** — *Theorycrafter tree masteries + clean nodes + honest estimates (Step 48, QA Riccardo).* Four QA points in one pass: (1) masteries weren't allocated — the loader dropped `masteryEffects`; now kept + allocated (best effect per build, capped 8, regular fill reduced to leave room). (2) wrong-weapon nodes ("damage with axes" on a sword build) — `_excluded_weapon_ids` drops every passive boosting an unused weapon class, applied to scoring + fill + bfs travel. (3) frontend showed un-rollable mods (phys damage on helmet) — `_rollable_priorities` filters `stat_priorities` per slot via the real-mod resolver, with multi-candidate stems for local/global (ES flat vs %, attack speed/accuracy local vs global → re-extracted mods to 21 stats). (4) life estimate ~13k was a `100*99` bug → realistic ~5k formula; the fake DPS index zeroed + hidden (real DPS needs PoB). Explicitly NOT done (deferred, noted in CLAUDE.md): notable-efficiency optimisation (preferring a +all-max-res notable over two single-res points) — that's Steiner-style and a separate effort.
- **2026-05-22** — *Theorycrafter real item mods shipped (Step 47, QA Riccardo).* Closes the last of the three QA complaints ("usa SOLO oggetti reali con i relativi modificatori"). Approach: vendor a *focused* slim of RePoE — not the whole 33 MB `mods.json`, just the item-domain prefix/suffix single-stat mods for the ~18 stats the generator recommends (~40 KB output). Resolution maps our recommendation stems → real PoE stat ids, then applies PoE's real spawn-weight gating (first matching tag or `default`, weight > 0) so a mod only appears on slots it can actually roll on, picks the best tier within the budget's ilvl cap, and renders the real value. `_theory_item_body` emits real-only (drops un-rollable priorities rather than inventing); the simulated `_AFFIX_VALUES` table is deleted. Local-vs-global was handled implicitly by the spawn gating (local crit/phys only resolve on weapons). Note: showing the tier's MAX value (best-case real roll) for PoB-parse safety rather than a "(min–max)" range.
- **2026-05-22** — *Theorycrafter tree pathing localised (Step 46, QA Riccardo).* Investigation came first and changed the diagnosis: the "disconnected / 108 points / Pathfinder nodes" report did NOT reproduce — all 3337 of our node IDs are present in PoB Community's 3.28 `tree.lua` (no data drift), and every build's allocation is one connected component (0 islands). The real defect was *sprawl* (Ranger pathing a median 24 hops out toward the Marauder/Witch side) plus a latent leak where `bfs_path` could route a waypoint segment *through* a mastery/cluster node. Fix: locality penalty `score - 0.7*hops` on waypoints + fill, regular-only BFS distances, `bfs_path` forbids non-regular nodes, dropped the ascendancy-entry connection (it pulled a non-regular node onto the path). The earlier ascendancy-float fix (commit `72dcba6`) had already removed the most visible "floating nodes" symptom. Next: real item mods via RePoE.
- **2026-05-22** — *Theorycrafter gem-link bug fixed (QA Riccardo).* Root cause was data, not logic: `extract_gems.py` dropped every SkillType outside a curated allowlist, and the discriminating types a support's `requireSkillTypes` depends on (Trapped, MeleeSingleTarget, Multistrikeable) lived in that dropped set — so the require list collapsed to empty, which PoB reads as "universal support". Fix keeps the full vocabulary + PoB's real any-of matching, with a `_SUPPORT_DMG_LOCK` for stat-based element/phys locks (Brutality etc.) the tag system can't express, and a `_CORE_SUPPORTS` global ranking for sensible ordering. Also: strict `support = true` (drops Tinctures misfiled in `sup_*.lua`), Awakened level cap (5), and excluded ascendancy notables from the encoded tree nodes (they floated). **Deliberately left for follow-up** (logged in §6 NEEDS DESIGN): tree-pathing locality (the allocation can sprawl toward far high-scoring nodes — possibly also a tree-data-version drift with the user's PoB) and real item mods (needs RePoE mods vendoring). Shipped the verified gem + tree-ascendancy + level fixes; flagged the two larger items rather than half-doing them.
- **2026-05-22** — *UX fix shipped (QA Riccardo).* Two friction points removed. (1) Finder was a deliberate two-step (extract-then-recommend) so the user could review/adjust the parsed intent before searching — but in practice the extra click felt like a bug. Now extract auto-chains into recommend; the filter row's button is kept for the *refine* case (re-recommend without re-extracting). Key gotcha: `recommendMut.mutationFn` had to take the intent as an argument — chaining off `onSuccess` while reading the still-stale store `intent` would have recommended against the *previous* query. (2) The `editing`-flag collapse-to-`<Code>` pattern (shared by Finder/Analyze/Planner) was the source of the "why do I have to click edit" complaint — replaced with an always-editable input, helper text hidden post-result. The `editing` store field is left in place (still written, just no longer gates display) to avoid a store-type migration.
- **2026-05-22** — *Step 45d shipped.* Closes the Step 45 quartet. Two implementation notes beyond the prompt sketch: (1) `Critical Strike Multiplier` had to be ordered *before* the existing `critical strike` keyword in `_AFFIX_VALUES` — `_affix_line` does substring matching and returns the first hit, so the multiplier line would be shadowed by the crit-chance line otherwise. (2) The weapon `_stat_priorities` call was changed to pass `weapon_label` (the actual `"Wand"`/`"Bow"`/`"Weapon"`) instead of the hard-coded `"Weapon"`, so the per-type map entry resolves correctly — the prompt's MAP differentiated those three but the call site didn't. Manual-PoB verification deferred to Riccardo; programmatic check (`test_stat_priorities_are_slot_aware`) covers the slot-awareness + flask-magic + no-"Theorycrafted" invariants.
- **2026-05-22** — *Step 45c shipped.* The prompt's allowlist used `" Support"`-suffixed names (`"Awakened Empower Support"`), but the vendored `gems_3_28.json` — extracted from PoB Community's `*.lua` — stores Awakened supports **without** that suffix (`"Awakened Empower"`). Verified by dumping the catalogue before coding: all 38 Awakened gems are suffix-free. Using the prompt's strings verbatim would have made `_is_available_in_328` block *every* Awakened gem (none would be in the allowlist), nuking Empower/Enlighten/Enhance too. Used the real names instead. Filter placed in `_select_supports_raw` (the single chokepoint after Step 45b) so both name-returning and object-returning callers inherit it; `_assert_valid` got a belt-and-braces guard.
- **2026-05-22** — *Step 45b shipped.* Both gem-layout bugs fixed as scoped. Two judgement calls beyond the prompt's sketch: (1) the Boots movement skill is now chosen to differ from *both* the primary and the helmet secondary — without this guard a melee build got Leap Slam in the helmet (secondary) and Leap Slam again in the boots (movement), a new duplicate the dedup step would otherwise have introduced. (2) The connected `test_no_duplicate_primary_skill` also asserts no active is repeated *anywhere* in the 5-link layout, not just the primary, catching the Leap Slam case. `_select_supports` kept as a thin name-returning wrapper over the new `_select_supports_raw` so `generate_build` (which still wants names) is unchanged. Verified Marauder/Juggernaut Earthquake → Earthquake / Leap Slam / Hatred / Flame Dash / Enduring Cry, all distinct.
- **2026-05-22** — *Step 45a shipped.* Implemented exactly as scoped: Part A (top-16 notable + top-4 keystone waypoints) + Part B (`_fill_to_budget` greedy frontier expansion). One judgement call beyond the prompt: the final output loop classifies nodes by `TreeNode` flags (`is_keystone`/`is_notable`/else travel) instead of by waypoint-target-set membership — the fill phase legitimately adds notables/keystones that were never waypoints, so the old set-membership tagging would have mislabelled them all as travel. The connected-test was relaxed from "adjacent to the *previous* node" to "adjacent to *some earlier* node" because `_fill_to_budget` appends boundary nodes adjacent to any visited node, not necessarily the immediately-preceding list entry. Result: 120 path nodes for a real intent (was ~9-20), hitting the `_MAX_TREE_NODES` cap.
- **2026-05-22** — *Step 45a scoped (research by Perplexity 2026-05-22).* Il budget reale di nodi allocabili dovrebbe essere ~100 (su `_MAX_TREE_NODES = 120`). Il problema attuale: `_select_tree_nodes` usa solo 10 target (8 notable + 2 keystone) come waypoints BFS. Se questi 10 sono geograficamente vicini sull'albero, il path totale rimane 15-20 nodi. Soluzione a due livelli: (A) espandere i target a ~20 nodi (top-16 notable + top-4 keystone) per forzare BFS su più territorio; (B) aggiungere un passo di "fill" che satura il budget con i nodi meglio-scored raggiungibili dall'albero già costruito, via iterazione BFS sul boundary. Il fill opera sul grafo già connesso (non spawna isole), garantisce adiacenza, e si ferma quando `_MAX_TREE_NODES` è raggiunto o il boundary è esaurito.
- **2026-05-20** — *Steps 43+44 shipped.* Step 43 ships exactly the 6 checks Perplexity scoped (errors block, warnings inform); no scope creep. Step 44 implementation note: the **first** working draft used a `dict.fromkeys` dedup at the end of the waypoint walk and broke adjacency on the Marauder/Juggernaut integration test (when target N+1 was reached via a path that revisited an earlier-visited node, the dedup silently dropped a step). Fix: `bfs_path` now accepts a `forbidden: set[int]` argument; the waypoint loop passes `visited - {current}` so each segment routes strictly through unvisited nodes. No final-pass dedup. Bug + fix is the kind of thing the integration test (`test_select_tree_nodes_connected`) catches before any user sees it.
- **2026-05-20** — *Steps 43+44 architecture decided (research by Perplexity 2026-05-20).* The Build Generator currently produces builds that are technically non-crashing but not viable — nodes are scored by keyword but float unconnected, defence constraints are unchecked, and gem attribute requirements are ignored. Two-step fix:
  - **Step 43** adds a `ViabilityReport` + validation pass **after** generation, surfaced to the user as warnings/errors in the UI. This catches and communicates known deficiencies without breaking the current generation pipeline. No tree rewrite.
  - **Step 44** adds BFS/Dijkstra tree pathing **inside** `_select_tree_nodes` using the `adjacency` graph already loaded by `get_tree_data()`. Produces a contiguous, allocatable path from the class start node through the top-scored notables. Replaces the current flat node list with a real path. Step 44 depends on Step 43 (uses `ViabilityReport` for post-path validation).
  - BFS is sufficient (uniform edge cost); Dijkstra with cost=1 produces the same result. The `TreeData.adjacency` field is already a `dict[int, frozenset[int]]` symmetric graph — no new data loading needed.
  - Resistance check: 75% cap requires ~135% total in sheet (Elemental Weakness map mod). The tree alone contributes 0% to resistances — this is always a gear constraint. The viability report must flag this clearly rather than pretending the tree fixes it.
  - Life/ES floor (softcore endgame mapping): 4 000 HP minimum, 6 000 ES minimum. These are realistic for the `mid`/`endgame` budget tier; the `starter` tier gets softer thresholds (3 000 HP / 4 000 ES).
  - Defence layer check: at least 2 distinct layers required. Layers derived from keystone presence in the selected nodes (Acrobatics → evasion+dodge, Iron Reflexes → armour, Mind Over Matter → MoM, Chaos Inoculation → CI/ES) + defence_archetype.
  - Gem attribute requirements: each active/support gem has Str/Dex/Int requirements. Cross-check against the class's base attributes + tree attribute nodes. Flag gems whose requirements exceed available attributes by >20.

- **2026-05-29** — *Step 70 shipped — chest-forbidding-helmet socketing.* The Bringer of Rain voids the body; the 6L was socketed in that phantom body (fictional). `_relocate_no_chest` moves the primary to the helmet (legal + picks up the Bringer's free supports), applied as a final overlay (the greedy undervalued it / re-pathed worse mid-pipeline). Lacerate +24%, Cyclone +3.5%, now honest. Residual: optimiser could still pick Bringer for a spell build (low priority).
- **2026-05-29** — *Step 69 shipped — rare-body gem-level influence affixes.* Real RePoE "+1 to Level of Socketed Skill/Support Gems" (influence-gated, endgame-ilvl), led on the Body Armour where the 6L is socketed. +6% isolated, Vortex +16% (live body). Diagnosed: The Bringer of Rain's "Can't use Chest armour" voids the body on 4/5 builds (the main 6L should move to the helmet — new NEXT STEP #9). Main beneficiary: the live generator.
- **2026-05-29** — *Step 68 shipped — honest passive-point budget.* `trim_to_budget` drops lowest-value removable leaves until the build fits a level-100 budget (123 pts); builds encode at level 100. Arc was 144 points (fictional) → all served builds exactly 123. Honest cost: −8 to −11% DPS on the 3 over-budget builds. The over-budget debt from Steps 63/67 is closed.
- **2026-05-29** — *Step 67 shipped — reservation efficiency → real multi-aura.* `GemLink.extra_actives` + multi-active group + Enlighten in `_to_pob_gems`; `optimize_auras` paths generic reservation-efficiency notables (PoB ignores disconnected node stats — the decisive finding) bounded by `max_res_points`, runs last on the final build; reservation gate tightened to strict 0. Precomputed builds +18-50% real DPS, all reservation-positive. Budget caveat (over-budget points) deferred to NEXT STEP #5.
- **2026-05-20** — *Step 42 design decided.* Frontend-only. Gear cards on the Theorycrafter result panel become expandable: clicking/hovering reveals the simulated affix lines already present in `GearSlot.stat_priorities` + the budget-scaled values emitted by Step 41's `_theory_item_body`. Each card gets a Trade icon that opens `TradeSearchDialog` using the slot's `base_name` + `stat_priorities` as the item type + mod hints — same pipeline already used by Analyze and Planner. No new backend endpoint needed; `POST /fob/trade-url` accepts the existing `TradeUrlRequest` shape.
- **2026-05-20** — *Step 41 shipped — Build Generator v2 PoB export completeness.* All five bugs fixed in `theory/generator.py` + minimal surgical changes to `pob/encode.py` helpers (`_placeholder_item_body` accepts a pre-built multi-line body; items loop counts FLASK/JEWEL occurrences and labels them `Flask N` / `Jewel N`). `encode_pob_code` public contract unchanged. `TreeNode` extended with a real `stats: tuple[str, ...]` field loaded from the raw tree JSON (the missing piece that made all the previous bugs cascade). Anti-hallucination assertions now also validate active gem names, not just supports. Unknown gem names degrade to `(open)` placeholders rather than tripping the guard — same pattern as missing supports.
- **2026-05-20** — *Step 41 QA identified five structural bugs in Build Generator v2 PoB export.*
- **2026-05-20** — *Step 40 shipped — gem data sourced from PoB Community upstream.* `scripts/extract_gems.py` — 555 actives + 278 supports. First successful run.
- **2026-05-20** — *No fallbacks to hand-authored data (permanent rule, §4 rule 7).*
- **2026-05-20** — *Build Generator v2 architecture decided.* Form-driven, graph engine, no free text, PoB XML complete, Trade per slot.
- **2026-05-19** — *Step 39 shipped — Theorycrafter Build Generator v1.* Superseded by v2.
- **2026-05-19** — *Step 38r reset executed (Prompt 025) — Option C chosen.*
- **2026-05-19** — *View Transitions API: route-level use rejected for good. Do not retry.*
- **2026-05-18** — *Trade prefill via backend; `?redirect&source=` abandoned.*
- **2026-05-18** — *Zustand for cross-route state persistence.*
- **2026-05-15** — *Full frontend redesign: "Void Stone & Ember".*
- **2026-05-14** — *Dynamic synthesis over curated templates. No PostgreSQL, no ETL.*
- **2026-05-07** — *Backend migrated Fly.io → Render.*

---

## 8. Prompt library

Reusable templates. Self-contained — runnable today without past-chat context. When a prompt ships, move to §9.

---

_(no prompts queued — awaiting the next from Perplexity)_

---

## 9. Prompt archive

Closed prompts kept for context. Don't run these.

- **Old Prompt 001–009** — Rejected or shipped 2026-05-14/15. See git history.
- **Old Prompt 010 (Step 22a — Void Stone & Ember design system)** — Shipped 2026-05-15. ✅
- **Old Prompt 011 (Step 22b — Finder page redesign)** — Shipped 2026-05-15. ✅
- **Old Prompt 012 (Step 22c — Planner timeline + Analyze polish)** — Shipped 2026-05-15. ✅
- **Old Prompt 013 (Step 23 — Parchment light mode)** — Shipped 2026-05-15. ✅
- **Old Prompt 014 (Step 24 — Finder result-list polish)** — Shipped 2026-05-18. ✅
- **Old Prompt 015 (Step 25 — Trade redirect on Planner gear + Analyze equipment)** — Shipped 2026-05-18. ✅
- **Old Prompt 016 (Step 26 — Route-level code-splitting)** — Shipped 2026-05-18. ✅
- **Old Prompt 017 (Step 27 — QA batch fixes + Zustand state persistence)** — Shipped 2026-05-18. ✅
- **Old Prompt 018 (Step 28 — Trade redirect v2: prefilled URLs)** — Shipped 2026-05-18. ✅ QA failed → fixed in Prompt 019.
- **Old Prompt 019 (Step 29 — Trade redirect 403 fix + Planner input parity)** — Shipped 2026-05-18. ✅
- **Old Prompt 020 (Step 33 — Visual polish batch 1)** — Shipped 2026-05-18. ✅
- **Old Prompt 021 (Step 34 — Visual polish batch 2)** — Shipped 2026-05-19. ✅
- **Old Prompt 022 (Step 35 — Visual polish batch 3)** — Shipped 2026-05-19. ✅
- **Old Prompt 023 (Step 36 — View Transitions API)** — Shipped 2026-05-19, partially reverted. ✅
- **Old Prompt 024 (Step 37 — Theorycrafter design & architecture analysis)** — Shipped 2026-05-19. ✅
- **Old Prompt 025 (Step 38r — Theorycrafter architectural reset)** — Shipped 2026-05-19. ✅ Option C chosen.
- **Old Prompt 026 (Step 39 — Theorycrafter Build Generator v1)** — Shipped 2026-05-19. ✅ Superseded by Prompt 027.
- **Old Prompt 027 (Step 40 — Theorycrafter Build Generator v2)** — Shipped 2026-05-20. ✅ Form-driven UI, graph engine, anti-hallucination asserts. 727 tests / 128 mypy.
- **Old Prompt 028 (Step 41 — Build Generator v2: PoB export completeness)** — Shipped 2026-05-20. ✅ Fixed five structural bugs: tree scoring uses `node.stats`; 5 gem groups (Body/Helmet/Gloves/Boots/Weapon); items ship simulated budget-scaled affix lines; 5 flask + 2 jewel slots labelled correctly in `<ItemSet>`; class start node uses the right class index. Encoder contract unchanged. 732 tests / 129 mypy.
- **Old Prompt 029 (Step 42 — Theorycrafter gear card UX + Trade dialog)** — Shipped 2026-05-20. ✅ Expandable gear cards (per-card `useState`), simulated affix list with sigils + `~ stimato` label, `TradeSearchDialog` opened per slot from parent state, two-column Grid layout on `md+`. Frontend-only. 732 tests / 129 mypy.
- **Old Prompt 030 (Step 43 — Build Generator viability validation pass)** — Shipped 2026-05-20. ✅ New `theory.viability` module; `validate_build` returns a `ViabilityReport` attached to every `BuildSkeleton`; 6 checks (gear-only resistance reminder; life/ES floors per budget; defence layer count via keystones; movement skill; mana sustain via flask or Lifetap). `ViabilityPanel` in the result with green/amber/red alert states. 741 tests / 131 mypy.
- **Old Prompt 031 (Step 44 — Build Generator BFS tree pathing)** — Shipped 2026-05-20. ✅ `bfs_path` module-level helper + visit-tracking greedy waypoint expansion in `_select_tree_nodes` — every consecutive node pair in the result is now adjacent in `TreeData.adjacency`. `TreeNodeRef.type` gains `"travel"`; frontend filters travel nodes out of the displayed tree-milestones list and adds a footnote with total + path-node counts. Initial dedup-at-the-end implementation broke adjacency; fix: `bfs_path` now takes a `forbidden` set so each segment routes strictly through unvisited nodes. 747 tests / 132 mypy.
- **Old Prompt 032 (Step 45a — Build Generator: tree node budget reale ~120 nodi)** — Shipped 2026-05-22. ✅ Waypoint targets expanded to top-16 notable + top-4 keystone; new module-level `_fill_to_budget` greedy boundary-expansion (best-scored frontier node, ties on lowest id) saturates `_MAX_TREE_NODES` while preserving the single connected component. Output classified by `TreeNode` flags (keystone/notable/travel). Connected-test relaxed to "adjacent to some earlier node". Marauder/Juggernaut Cyclone → 120 path nodes (92 travel). `bfs_path`/`_score_node`/frontend untouched. 749 tests / 132 mypy.
- **Old Prompt 033 (Step 45b — Build Generator: gem layout deduplicato + support compatibili)** — Shipped 2026-05-22. ✅ Bug 1: Helmet 4L now hosts a secondary skill (`_SECONDARY_SKILL` + `_pick_secondary`), not the duplicated primary; boots movement avoids primary + secondary. Bug 2: `_select_supports` split into `_select_supports_raw` (tag-filtered `_Support` objects) + `_pick_supports_for(skill, prefer, n)` (compatible-only prefer + compatible-pool fill). No active repeated across the 5 links. 751 tests / 132 mypy.
- **Old Prompt 033b (Step 45c — Build Generator: Awakened gem allowlist 3.28)** — Shipped 2026-05-22. ✅ `_AWAKENED_ALLOWLIST` (Empower/Enlighten/Enhance, no `" Support"` suffix — matched to the real catalogue strings) + `_is_available_in_328` filter in `_select_supports_raw` + guard in `_assert_valid`. Non-allowlisted Awakened gems no longer appear in any link. 752 tests / 132 mypy.
- **Old Prompt 033c (Step 45d — Build Generator: item reali con stat priority per slot)** — Shipped 2026-05-22. ✅ `_stat_priorities` rewritten as an explicit per-slot map (spell/attack, ES/life, crit-aware); `_AFFIX_VALUES` extended with Cast Speed / Flask Life Recovery / Mana / all Attributes / Accuracy / Block / Crit Multi (multi ordered before "critical strike"); `_theory_item_body` "Theorycrafted"→"Generated" + flasks rendered as MAGIC `<base> <suffix>` via `_FLASK_SUFFIX`; weapon priorities keyed on `weapon_label`. 753 tests / 132 mypy.
